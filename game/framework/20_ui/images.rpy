# Служебные образы framework. Игровые образы сюда не попадают никогда —
# их эмитит компилятор из Asset Registry (раздел 2).

# Нейтральный фон сцен до подключения ассет-пайплайна (обвязка сцен, scenes.py).
image vn_black = Solid("#000000")
