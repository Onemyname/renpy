// Шаблон app_build для steamcmd (ci/steam/README.md). Подстановки делает
// vn release steam; руками файл не редактируется — правьте project.yaml.
"AppBuild"
{
	"AppID" "{APPID}"
	"Desc" "{DESC}"
	"SetLive" "{BRANCH}"
	"ContentRoot" "{CONTENT_ROOT}"
	"BuildOutput" "{BUILD_OUTPUT}"
	"Depots"
	{
{DEPOTS}
	}
}
