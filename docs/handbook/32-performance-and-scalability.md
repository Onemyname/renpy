# 32. Производительность и масштабирование

> **Статус подсистемы:** PARTIALLY IMPLEMENTED — из пяти бюджетов G19 четыре размерных проверяются в `vn build` и в релизном гейте (`release.py:29-54`), пятый (`cold_start_s`) — **только** внутри `vn test smoke`, которого нет ни в `ci.yml`, ни в релизном гейте. Всё остальное из G19 (`ARCHITECTURE.md:89`: baseline RSS, суммарный размер `.rpyc`, размер финальных `.aab`/`.apk`) — **NOT IMPLEMENTED**; команды `vn test perf` не существует даже как заглушки.
> **Отвечает на вопрос:** «Сколько это сейчас стоит по времени и мегабайтам, что сломается первым при росте до 50 глав / 50 персонажей / 10 языков и какие решения нельзя откладывать».

Проект сейчас — 2 главы, 4 сцены, 1 персонаж, 20 собранных ассетов, 115 строк локализации. На этом объёме перф-вопросов нет: `vn build` на прогретом кэше — 0,3 с, весь генерат — 73,3 КБ при бюджете 2048 КБ, дистрибутив — 30 МБ, из которых 97 % занимает сам движок. Этот файл — про то, где именно арифметика ломается при росте и какие решения дороже всего откатывать потом.

## Быстрый ответ

```bash
vn build                    # бюджеты G19 проверяются на каждой сборке (cli.py:142,152)
vn release validate --flavor public   # 19 проверок, среди них бюджеты G19 (#9) и видео (#7)
vn test smoke --picks 0,0   # единственное место, где меряется cold start (cli.py:1384-1392)
vn assets cache --dry-run   # сколько мусора накопил контентно-адресуемый кэш
```

Измерить фактические размеры прямо сейчас:

```bash
python -c "from pathlib import Path; s=lambda p: sum(f.stat().st_size for f in Path(p).rglob('*') if f.is_file()); \
print('generated', s('game/generated')/1024, 'КБ'); print('assets', s('game/assets')/1048576, 'МБ'); \
print('tl', s('game/tl')/1024, 'КБ'); print('vncache', s('.vncache')/1048576, 'МБ')"
```

Три числа, которые упрутся раньше остальных: **`generated_total_kb: 2048`** (≈20 реальных глав), **`--timeout 180` у `vn test smoke`** (≈1 реальная глава), **порог ADR-0004 в 50 МБ бинарей в `assets_src/`** (≈10 реальных PNG-рендеров).

---

# Часть 1. Перформанс сегодня

## 1. Бюджеты G19: пять чисел и где каждое проверяется

Источник — `../../project.yaml:6-11`. Единая реализация размерных бюджетов — `budget_failures()` (`../../tools/vn/src/vn/release.py:29-54`), она читает `project.yaml: budgets` и возвращает список нарушений.

| Бюджет | Значение | Что меряется | Где проверяется | Что значит превышение |
|---|---|---|---|---|
| `assets_total_mb` | 500 | `_dir_size(game/assets)` | `vn build` (`cli.py:142`, `:152` → `_check_budgets` `cli.py:172-180`) и гейт (`release.py:361-363`) | `_fail("бюджеты G19 превышены")` → **exit 1**, красная сборка |
| `generated_total_kb` | 2048 | `_dir_size(game/generated)` — **включая `.rpyc` и `manifest.json`** | там же | то же |
| `video_total_mb` | 300 | `_dir_size(game/assets/mov)` | там же | то же |
| `video_file_mb` | 40 | каждый `game/assets/mov/**/*.webm` | там же **плюс** второй раз внутри `validate_output` (`tools/vn/src/vn/assets/video.py:234-236`) — одно и то же нарушение может напечататься дважды разными словами | то же |
| `cold_start_s` | 30 | `startup.txt` из прогона автопилота | **только** `vn test smoke` (`cli.py:1384-1392`) | `_fail("cold start N c > бюджета 30 c (G19)")` |

**Что бюджетами НЕ покрыто** (все — NOT IMPLEMENTED):

| Не покрыто | Норма | Почему это важно |
|---|---|---|
| `game/tl/` | — | ни в один бюджет не входит; при 10 языках × 50 глав это десятки МБ `.rpy`+`.rpyc`, которые движок грузит на старте |
| Суммарный размер `.rpyc` | `ARCHITECTURE.md:89`, `:1601`, `:2391` | Ren'Py грузит AST всего скрипта при старте — это главный источник роста cold start, и он не измеряется |
| baseline RSS | `ARCHITECTURE.md:89`, `:3696` | джобы `nightly-perf` нет ни в одном workflow |
| Размер финальных артефактов (`.aab`, universal `.apk`, десктопные архивы) | `ARCHITECTURE.md:1189`, `:3787` | бюджет на каталог `game/assets` ничего не гарантирует: 97 % дистрибутива — движок (см. §6) |
| Бюджет памяти сцены (`decoded_mb`, 60 % от `image_cache_size_mb`) | `ARCHITECTURE.md:1234`, `:2409` | ни `decoded_mb`, ни trim-оптимизатор, ни predict-эмиссия в коде не существуют (grep по `tools/vn/src/`: 0 совпадений) |
| Профиль `draft` в релизе | — | в `release.py` **нет ни одной ссылки на `profile`** — черновой WebP q50 и CRF 42 пройдут гейт незамеченными |

### 1.1 Почему `cold_start_s: 30` — это про CI-раннер, а не про RTX

Комментарий в `project.yaml:9` фиксирует калибровку буквально: «референс — CI-раннер (llvmpipe): ~14 c; RTX — ~1 c». Причина в том, что Ren'Py **не бывает по-настоящему headless** — ему нужен GL-контекст (`ARCHITECTURE.md:3559`, норма G23), поэтому в CI он идёт под `xvfb-run` с программным рендером Mesa llvmpipe. Программный рендер тратит секунды там, где дискретная карта тратит миллисекунды: создание GL-контекста, компиляция шейдеров, первая загрузка текстур.

Практический вывод: **порог 30 с бессмысленно проверять локально** — на RTX 5080 фактический замер `.vncache/smoke/startup.txt` = `1.13`, то есть запас более чем 25-кратный. Порог существует ради nightly (`nightly.yml`) и weekly canary (`canary.yml`), где он ловит регрессии на референсно медленном железе. Если вы «оптимизировали старт» и проверили это только локально — вы ничего не проверили.

### 1.2 Что именно меряет `cold_start_s` (и чего он не меряет)

Механика — три точки в коде:

| Точка | Код |
|---|---|
| `_T0 = time.time()` в `init -999 python in vn_qa` | `game/framework/00_core/030_flow.rpy:96` |
| `startup.txt` пишется на **первом** тике автопилота (`n == 0`) | `030_flow.rpy:115-118` |
| тик приходит от `timer 0.6 ... repeat True` во внедряемом раннере | `../../tools/vn/src/vn/cli.py:1281` |

Отсюда три честных оговорки:

1. **У метрики есть пол ≈ 0,6 с** — период таймера. Фактические 1,13 с — это примерно 0,5 с реального старта плюс один такт таймера.
2. **Метрика начинается на `init -999`, а не при запуске процесса.** Всё, что до init-кода — запуск python, `Early init`, `Loading script` — в неё **не входит**. Именно `Loading script` растёт с числом `.rpyc` (см. §3), то есть главный механизм деградации старта бюджетом не покрыт.
3. Метрика существует только под автопилотом: в обычном `vn play` её никто не считает.

## 2. Реальные измерения (2026-08-08, RTX 5080 / Windows 11)

### 2.1 Фазы старта из `../../log.txt`

Это реальный лог движка, шапка — `Ren'Py 8.5.3.26051504`:

| Фаза | Время | От чего зависит |
|---|---|---|
| `Early init` | 22 мс | движок, не проект |
| `Loading error handling` | 10 мс | движок |
| **`Loading script`** | **67 мс** | **число и размер `.rpyc`** — весь `game/framework`, весь `game/generated`, весь `game/tl` всех языков |
| `Loading save slot metadata` | 15 мс | число слотов сейвов |
| **`Running init code`** | **21 мс** | объём кода в `init`-блоках: `00_core`, реестры `VN_*`, `layeredimage`, стили |
| `Initial gc` | 19 мс | суммарный объём объектов, созданных на init |
| **`Creating interface object`** | **112 мс** | движок + GL (на llvmpipe — секунды) |
| `Load screen analysis` | 10 мс | число и сложность экранов |
| **`Interface start`** | **91 мс** | движок + GL |
| Сумма **всех** фаз из `log.txt:5-32` (включая нулевые, не показанные выше) | **380 мс** | |

Читать так: **проектная часть старта сейчас — это 67 + 21 + 10 = 98 мс**, остальное — движок и GL. Растёт при росте контента именно эта часть, причём линейно по объёму `.rpyc` и init-кода.

### 2.2 Тулинг

| Операция | Время | Условие |
|---|---|---|
| `vn build` | **0,3 с** | прогретый кэш ассетов и прогретый `.vncache/analyze-*.json` |
| `python -m pytest tools/vn/tests -q` | **7,25 с** | 152 теста, 19 файлов |
| `vn test smoke` (демо-контент) | ~15 с | 19 тиков × 0,6 с + старт |

Холодная сборка дополнительно поднимает Ren'Py-мост (`renpy.exe <root> vn_analyze`, таймаут 300 с, `tools/vn/src/vn/content/analyze.py:57-61`) — отдельный запуск движка на **весь** набор сцен. Точное время здесь не замерялось; замерьте сами, удалив `.vncache/analyze-*.json` и засекя `vn build`.

### 2.3 Размеры на диске

| Зона | Размер | Файлов | Бюджет |
|---|---|---|---|
| `game/generated/` | **75 034 Б (73,3 КБ)** | 39 (19 `.gen.rpy` = 32 954 Б + 19 `.rpyc` = 36 682 Б + `manifest.json` 5 398 Б) | 2048 КБ → занято **3,6 %** |
| `game/assets/` | 144 719 Б (141,3 КБ) | 20 | 500 МБ → занято **0,03 %** |
| `game/assets/mov/` | 6 209 Б | 2 | 300 МБ |
| `game/tl/` (3 языка) | 44 691 Б | 21 | нет бюджета |
| `assets_src/` | 132 427 Б, из них бинарей 129,3 КБ | 11 бинарных | порог ADR-0004: warn 30 МБ / error 50 МБ |
| `.vncache/` | ~1,98 МБ | — | нет бюджета, GC только по `.vncache/assets` |
| `build/rpyc-cache/0.1.0/` | 159 395 Б | 48 `.rpyc` | нет бюджета |

## 3. Что дорого именно в Ren'Py (и как это устроено здесь)

| Стоимость | Как это выглядит в этом проекте | Статус механизма |
|---|---|---|
| **AST всего скрипта грузится на старте** | `Loading script` 67 мс; растёт с каждым `.gen.rpy`, каждым `dialogue_<ch>.rpy` каждого языка и каждым файлом `game/framework/` | бюджета на суммарный `.rpyc` нет — NOT IMPLEMENTED (`ARCHITECTURE.md:89`) |
| **Объём `init`-кода** | `Running init code` 21 мс. Крупнейшие init-потребители — реестры `VN_*` (`init -100`), `layeredimage` и `image` (`init offset = 0`, `images.gen.rpy:7`), стили `20_ui` | IMPLEMENTED, компактный кодоген: `chapter_select.gen.rpy` — **цикл по `VN_CHAPTERS`**, а не развёртка (ровно как требует `ARCHITECTURE.md:1601`) |
| **Число image-стейтментов** | сейчас 5 `image` + 1 `layeredimage`; каждый `image` — отдельный стейтмент базового приоритета 500 | IMPLEMENTED |
| **Декодированная память образов** | реальные размеры: спрайт-слой 1200×2200 → `1200*2200*4 = 10 560 000 Б = 10,07 МиБ`; фон/CG 1920×1080 → `7,91 МиБ` | считается только в этом файле — валидатора нет |
| **`config.image_cache_size_mb`** | задаётся из `project.yaml: render.image_cache_mb` через `generated/render.gen.rpy` (ADR-0012); худшая сцена гейтится `vn assets memory` | решение проекта |
| **Prediction** | предсказание не настраивается и не эмитится; `ARCHITECTURE.md:2391` называет prediction «главным узким местом» и требует predict-списков данными | NOT IMPLEMENTED |
| **Видео в памяти** | `Movie(...)` в галерее живёт ровно пока показан экран (`20_ui/screens/gallery.rpy:105-107`); в сценах видео сейчас не используется вообще | IMPLEMENTED (галерея) |
| **Rollback / история** | `config.hard_rollback_limit = 100` (`00_core/001_boot.rpy:9`), `config.history_length = 250` (`20_ui/screens/history.rpy:6`) | IMPLEMENTED; это единственные две перф-относящиеся константы рантайма |

### 3.1 Арифметика памяти на реальном персонаже

`mira` показывается как `layeredimage` из трёх одновременно декодированных слоёв (`game/generated/registry/images.gen.rpy:17-30`): `base` + один `outfit` + одна `face`.

```
слой 1200×2200 RGBA        = 10,07 МиБ
mira в кадре (3 слоя)      = 30,2 МиБ
+ фон 1920×1080            =  7,9 МиБ
сцена «один персонаж + фон»= 38,1 МиБ
сцена «четыре + фон»       = 128,7 МиБ
```

Дефолтный `image_cache_size_mb = 400` вмещает ~3 таких четырёхперсонажных сцены. Ren'Py при переполнении кэша не падает — он начинает молча перерасшифровывать образы посреди сцены (thrash). `ARCHITECTURE.md:2409` требует trim-оптимизатора (обрезка наряда по bbox: 47 → 26 МБ на персонажа) и валидатора worst-case — **ни того, ни другого в коде нет**.

Обратите внимание: суффикс `@2` — **чистая конвенция имени**, кода масштабирования в конвейере нет (`tools/vn/src/vn/assets/pipeline.py:121-137` собирает путь f-строкой, `_webp_encode` без `max_side` — `:223`). Движок отрисует спрайт в 600×1100 виртуальных пикселей, но **декодирует полные 1200×2200**. Экономии памяти `@2` не даёт — только резкость на HiDPI.

## 4. Оптимизация ассетов

Полная таблица трансформаций и правил именования — [Ассеты](16-assets.md); здесь только перф-грань.

| Тип | Параметр `full` | Параметр `draft` | Код |
|---|---|---|---|
| Спрайт-слой | WebP q95, без ресайза | q50 | `tools/vn/src/vn/assets/pipeline.py:223` |
| Фон | q90 | q50 | `:225` |
| CG | q90 | q50 | `:227` |
| Миниатюра CG | q80, длинная сторона 512 (LANCZOS) | **то же самое** — профиль игнорируется | `:228-231` |
| UI-панель | lossless, `method=4` | lossless, `method=0` (быстрее, **те же пиксели**) | `:237-248` |

Что из этого реально экономит:

- **Миниатюры галереи** — единственная реализованная оптимизация показа: сетка галереи не декодирует полноразмерные CG (`:229-231`). При 500 CG это разница между 512-пиксельными превью и 500 × 7,9 МиБ.
- **`--profile draft`** экономит **время энкода**, а не время игры. Это инструмент цикла художника: `vn dev` при правке `assets_src/` собирает в `draft` (`cli.py:243-246`), `vn assets watch` — тоже `draft` по умолчанию (`cli.py:551`).
- **Грабля:** профиль входит в ключ кэша (`pipeline.py:309-311`) и в сравнение свежести. После `draft`-сборки `vn build --check` объявит несвежими **все** выходы. Перед пушем — пересоберите `full`.

### 4.1 Модель стоимости `vn assets build` — важнее, чем параметры качества

Читайте цикл сборки как есть (`tools/vn/src/vn/assets/pipeline.py:288-357`):

```python
src_bytes = src.read_bytes()          # :293 — ВСЕГДА, для каждого источника
src_hash  = _b3_bytes(src_bytes)      # :298
...
if blob.is_file(): data = blob.read_bytes()      # :334-335 — попадание в кэш
if dest.is_file() and dest.read_bytes() == data: # :352 — сравнение с выходом
```

Следствия, которые определяют потолок роста:

1. **Даже полностью прогретая сборка читает каждый байт `assets_src/` с диска** — mtime/size-шортката нет, хэш считается по содержимому. Плюс читается блоб кэша и читается текущий выход. Итого объём чтения ≈ `сырцы + 2 × выходы` **на каждый вызов**.
2. **Никакого параллелизма.** Ни `multiprocessing`, ни `ThreadPool`, ни `concurrent.futures` в `tools/vn/src/` нет ни одного вхождения. Флаг `--jobs N` существует только в `ARCHITECTURE.md:1242,3517` — **NOT IMPLEMENTED**; у `vn assets build` есть ровно одна опция, `--profile` (`cli.py:515-519`).
3. **Нарезка PSD не инкрементальна**: `slice_all_psd` вызывается безусловно в начале каждой не-`check` сборки (`pipeline.py:266-268`) и режет каждый PSD целиком; кэшируется только последующий WebP-энкод. Требование послойной инкрементальности и бюджета «P95 < 15 с на PSD до 500 МБ» (`ARCHITECTURE.md:1204`) — NOT IMPLEMENTED.
4. **Кэш растёт без границ**: каждая правка сырца оставляет прошлый блоб в `.vncache/assets/<2hex>/<64hex>` навсегда. Подметает только `vn assets cache --gc` (mark & sweep от манифеста, `pipeline.py:458-491`). `.vncache/video-tmp/` не подметается вообще.

Прикидка: 5000 спрайт-слоёв по 8 МБ = 40 ГБ сырцов. No-op сборка прочитает ~40 ГБ + 2×(выходы). На NVMe 3 ГБ/с это ~15 с; на SATA-SSD 500 МБ/с — ~80 с. Цифры сырцов здесь — допущение (реальный вес меряется после первой партии рендеров), но **линейность по объёму и однопоточность — факты из кода**.

## 5. Оптимизация видео

Полный пресет, валидация и сайдкары — [Генерация видео](21-video-generation.md). Перф-грань:

| Рычаг | Значение | Код | Что даёт |
|---|---|---|---|
| `crf` | 30 (full) / 42 (draft); сайдкар может переопределить, схема допускает 4..63 | `tools/vn/src/vn/assets/video.py:91,94`, `tools/schemas/video_src@1.schema.json` | главный рычаг размера |
| `-cpu-used` | 2 (full) / 8 (draft) | `video.py:92,95` | время энкода, не размер файла |
| Потолок высоты | `max_height or 1080`; draft ужимает до `min(max_h, 720)` | `video.py:88,90` | размер ∝ площади кадра |
| Длительность лупа | предупреждение при > `MAX_LOOP_DURATION_S = 30.0` | `video.py:30,239-241` | размер ∝ длительности |
| Шов лупа | RMS первого/последнего кадра, warn при > `LOOP_SEAM_WARN = 18.0` | `video.py:29,244-246` | качество, не размер |
| Аудио | вырезается (`-an`), если нет `keep_audio: true` | `video.py:107-110` | ~96 кбит/с экономии |

Бюджеты: 40 МБ на файл и 300 МБ суммарно. При типичном 10-секундном 1080p-лупе на CRF 30 (единицы МБ) бюджет `video_total_mb` упрётся в районе **нескольких десятков лупов**. Точное число зависит от контента — считайте `du game/assets/mov` после первой партии.

**Два реальных разрыва:**
- Кодировка **однопроходная**, 2-pass нет нигде в коде; профили `hd`/`mobile` из `ARCHITECTURE.md:1179` — NOT IMPLEMENTED.
- `encode_video` вызывает `subprocess.run` **без `timeout=`** (`video.py:121`) — зависший ffmpeg вешает сборку насмерть, хотя соседний `pipeline._run_out` таймауты использует.

## 6. Размер дистрибутива и вопрос `.rpa`

Реальный разбор `build/dist/0.1.0-public/vn-0.1.0+94970b3-win.zip` (версия 0.1.0, то есть более раннее состояние контента; 31 489 064 Б на диске = 30,03 МиБ):

| Часть | Распакованный размер | Файлов |
|---|---|---|
| `lib/` (py3-windows-x86_64 + python3.12) | **61,88 МБ** | 941 |
| `renpy/` | **11,68 МБ** | 554 |
| `game/` | **2,04 МБ** | 143 |
| `vn.exe` + `vn.py` | 0,11 МБ | 2 |
| **Итого** | **75,71 МБ** → 30,03 МиБ в zip | 1640 |

Внутри `game/`: `fonts/` 1,108 МБ (три TTF — крупнейший игровой актив), `cache/` 0,598 МБ (байткод-кэш движка), `framework/` 0,173 МБ, `assets/` 0,067 МБ, `generated/` 0,055 МБ, `tl/` 0,022 МБ.

**Вывод: 97 % дистрибутива — движок и python.** Пока `game/assets` не перевалит за сотню мегабайт, любая «оптимизация размера сборки» на стороне контента — шум.

Что вырезает `build.classify(..., None)` (`../../game/options.rpy:17-26,29-51`): все исходные зоны (`tools/ content/ assets_src/ loc/ docs/ ci/ packs/ build/ .vncache/ .git/`), дотфайлы, `README.md`, `project.yaml`, `log.txt`/`traceback.txt`/`errors.txt`; затем release-профиль — `game/framework/90_debug/**`, `game/generated/qa/**`, `game/generated/manifest.json`; затем synthetic-языки по `language.json` (сейчас режет `pseudo`); затем флейворные NSFW-глобы из `build_id.json`.

### 6.1 Дадут ли выигрыш `.rpa`-архивы

`build.archive(...)` в проекте **нет ни одного вызова** — `.rpa` NOT IMPLEMENTED (`ARCHITECTURE.md:943` их требует). Оценка по факту формата:

- RPA-3.0 хранит **индекс** сжатым zlib и обфусцированным XOR-ключом, а сами данные — сырыми срезами `(offset, dlen)` (`$RENPY_SDK/renpy/loader.py:157-181`). **Пофайлового сжатия у `.rpa` нет.**
- Содержимое `game/assets/` — WebP и WebM, то есть уже сжатые форматы. **Выигрыш по размеру ≈ 0.**

Что `.rpa` реально даёт: (а) один файл вместо тысяч — быстрее распаковка инсталлятором, меньше проверок антивирусом, меньше записей в файловой таблице; (б) барьер против «щёлкнул правой кнопкой — вытащил все CG»; (в) удобную единицу поставки DLC. Что забирает: пересборка всего архива при правке одного ассета и патч размером с архив вместо размера файла.

**Вердикт: не сейчас.** Разумный порог — когда число файлов в `game/assets/` перевалит за тысячи или когда паки начнут поставляться отдельно (см. [Паки и DLC](30-packs-and-dlc.md)). Приоритизация — [Роадмап](37-roadmap.md).

---

# Часть 2. Масштабирование

## 7. Главы: 1 → 5 → 10 → 20 → 50+

### 7.1 Ключевой факт: инкрементальности по главам нет

Проверено по коду, формулировка точная:

| Механизм | Как есть | Код |
|---|---|---|
| Компилятор | **все** выходы пересчитываются в память на каждый вызов; на диск пишутся только изменившиеся байты | `tools/vn/src/vn/content/compile.py:899-910` (запись), `:882-890` (`--check`) |
| Ren'Py-мост (парсинг авторских `.rpy`) | ключ кэша считается по **всему набору** файлов сразу: `for f in sorted(files): update(path); update(bytes)` | `tools/vn/src/vn/content/analyze.py:42-53` |
| Свежесть генерата | побайтовое сравнение выходов; `manifest["inputs"]` пишется, но **никогда не читается обратно** | `compile.py:882-886`, `:912-922` |
| Вотчер `vn dev` | `rglob("*")` + `stat()` по всему `content/` и `assets_src/` **раз в секунду** | `devloop.py:15-28`, `:31-56` |

Практический смысл второй строки: **правка одной реплики в одной сцене инвалидирует AST-кэш всех сцен проекта** и запускает полный перепарс через один процесс `renpy.exe vn_analyze`. Это самый важный будущий боттлнек компиляции — и самое дешёвое исправление (ключ на файл вместо ключа на набор).

Плюс: файлы `.vncache/analyze-*.json` не подметаются ничем — `cache_gc` знает только `.vncache/assets` (`pipeline.py:462`).

### 7.2 Когда упрётся `generated_total_kb: 2048`

Формула выводится из реальных файлов. Сгенерированная сцена = шапка + обвязка + **дословная копия авторского `.rpy`** (`tools/vn/src/vn/content/scenes.py:270-271`). Замер: `s010_intro.scene.rpy` = 676 Б → `ch01_s010.gen.rpy` = 1997 Б, то есть фиксированная часть ≈ 1,3 КБ. Отношение `.rpyc`/`.rpy` по четырём реальным сценам — 1,39 / 1,53 / 1,41 / 1,46, в среднем **≈ 1,45**.

```
вес одной сцены в game/generated ≈ (1,3 КБ + размер авторского .rpy) × 2,45
```

| Сценарий | Авторский `.rpy` | Пара `.rpy`+`.rpyc` | 10 сцен = глава | Бюджет 2048 КБ упрётся |
|---|---|---|---|---|
| Демо (как сейчас, ~4 реплики) | 0,7 КБ | 4,9 КБ | 49 КБ | ~40 глав |
| Реальная сцена, 30 реплик | 2,4 КБ | 9,1 КБ | 91 КБ | **~21 глава** |
| Плотная сцена, 80 реплик | 6,4 КБ | 18,9 КБ | 189 КБ | ~10 глав |

Плюс растут реестры: `menus.gen.rpy` уже 6 941 Б (95 UI-строк + подписи пунктов меню), `gallery.gen.rpy` 3 261 Б. Реалистичный ориентир — **бюджет `generated_total_kb` покраснеет между 10 и 20 реальными главами**.

Что делать, когда упрётся (по возрастанию цены):
1. Поднять число в `project.yaml:8` — это осознанное решение, а не обход: бюджет существует ради cold start, и вместе с ним надо завести замер `Loading script` (сейчас его нет).
2. Перестать копировать авторский источник в генерат (`scenes.py:270-271`) — минус ~40 % объёма, ценой того, что генерат перестанет быть самодостаточным для чтения в отладке.
3. Ввести бюджет на суммарный `.rpyc` отдельно от каталога (`ARCHITECTURE.md:89`) — правильное решение, потому что мерить надо именно то, что грузится на старте.

### 7.3 Когда сломается smoke

`vn test smoke` играет игру **целиком**: от `label start` (`030_flow.rpy:217`) по цепочке `exits` до `vn_end_of_content`. Продвижение — `renpy.queue_event("dismiss")` раз в 0,6 с (`030_flow.rpy:128`, таймер `cli.py:1281`), выбор в меню — раз в 1,0 с (`20_ui/screens/choice.rpy:54`). Каждый тик — скриншот PNG.

```
длительность прогона ≈ (число реплик + число меню) × 0,6 с
```

Замер демо: 19 тиков, 19 PNG на 1,0 МБ (в среднем 54,6 КБ на плейсхолдерной графике).

| Объём | Длительность прогона | Скриншотов | Вердикт |
|---|---|---|---|
| Сейчас (15 реплик, 2 меню) | ~15 с | 19 / 1 МБ | норма |
| 1 реальная глава (300 реплик) | **~3 мин** | ~300 | **пробивает `--timeout 180` по умолчанию** (`cli.py:1350`) |
| 5 глав | ~15 мин | ~1500 / сотни МБ артефакта | nightly ещё терпит |
| 10 глав | ~30 мин × 4 прогона матрицы (`nightly.yml:55-60`) = 2 часа | ~3000 | не терпит |
| 50 глав | ~2,5 часа на **один** прогон | ~15 000 | невозможно |

Дополнительно ломается `--picks`: это плоский список индексов «по одному на меню в порядке появления» (`030_flow.rpy:137-140`), при сотнях меню он нечитаем и неподдерживаем.

**Решения и когда их принимать:**
- **На первой реальной главе:** передавать `--timeout` явно везде, где есть smoke; в `nightly.yml:55-60` таймаут не передаётся вообще.
- **На 3–5 главах:** шардировать прогон. Механизм уже есть в коде — `VN_AUTOPILOT_LOAD` грузит сейв на старте (`030_flow.rpy:162-164`), а `vn save corpus --add` умеет делать фикстуры (`cli.py:1185-1213`). То есть «фикстура на вход каждой главы + прогон от неё» реализуем без нового рантайма, нужна только CLI-обвязка.
- **На 10+ главах:** `vn test smoke --affected --shard N/M --seed` из `ARCHITECTURE.md:3530,3587` — NOT IMPLEMENTED; реальный `test smoke` принимает только `--picks`, `--lang`, `--timeout`.
- **Всегда:** артефакт `.vncache/smoke/` уходит в GitHub с retention 7 дней (`nightly.yml`) — при тысячах PNG это гигабайты; понадобится отбор (первый/последний кадр сцены), а не «всё подряд».

### 7.4 Остальное, что растёт с числом глав

| Механизм | Как масштабируется | Порог | Код |
|---|---|---|---|
| `vn content lint` | схема-валидация всех деклараций O(N); достижимость — BFS **на главу**, поиск `chapter.yaml` линейным перебором всех документов на каждую главу (O(глав × документов)) | 50 глав × ~600 документов = 30 тыс. итераций — незаметно | `tools/vn/src/vn/content/lint.py:257-295` |
| `vn content graph` | один Mermaid-`flowchart TD` со **всеми** главами и сценами; пак-главы **не попадают** (сканируется только `content/chapters`) | Mermaid деградирует на сотнях узлов; при 50 главах × 10 сцен = 500 узлов рендер в браузере уже болезненный. Нужен `--chapter` — его нет | `tools/vn/src/vn/content/graph.py:15` |
| `vn release changelog` | диффует только `content/chapters/`, пак-главы невидимы | сразу | `release.py:124-139` |
| Ren'Py `Loading script` | линейно по числу `.rpyc` | 67 мс на 4 сценах; не измеряется бюджетом | `log.txt:7` |
| Экран выбора глав | цикл по `VN_CHAPTERS`, не развёртка | масштабируется нормально; при 50 главах понадобится скролл/группировка в UI | `game/generated/screens/chapter_select.gen.rpy` |
| `id_registry.json` (G7) | заполняется **только** главами со `status: release`; сейчас `ch01` — `draft`, реестр пуст | защита от исчезновения id включится с первой release-главой | `release.py:69-121` |

## 8. Персонажи: 1 → 20 → 50+

### 8.1 Комбинаторика на реальном примере

`content/characters/mira/character.yaml:7-12` объявляет матрицу `poses: [a]`, `outfits: [school, casual]`, `emotions: [neutral, smile, angry]` — то есть **1 × 2 × 3**.

Ключевое различие, которое надо держать в голове: **файлы аддитивны, комбинации мультипликативны.**

```
файлов на персонажа   = poses × (1 base + outfits + emotions)
комбинаций на экране  = poses × outfits × emotions
```

Для `mira`: файлов `1 × (1+2+3) = 6` (проверено: `game/assets/spr/mira/a/` — `base@2.webp`, 2 наряда, 3 лица); комбинаций `1×2×3 = 6`.

| Персонаж | Матрица | Файлов | Комбинаций | Вес в `game/assets/spr` (при 1,5 МБ на слой) |
|---|---|---|---|---|
| `mira` сейчас | 1×2×3 | 6 | 6 | 47 КБ (плейсхолдеры) |
| Реалистичный второстепенный | 2×2×4 | 2×(1+2+4) = 14 | 16 | ~21 МБ |
| Реалистичный основной | 3×4×8 | 3×(1+4+8) = 39 | 96 | ~59 МБ |
| **20 персонажей (смесь)** | — | **~400 файлов** | — | **~600 МБ → бюджет `assets_total_mb: 500` пробит** |
| **50 персонажей** | — | **~1000+ файлов** | — | **1,5 ГБ+** |

Вес слоя 1,5 МБ здесь — допущение (WebP q95 с 1200×2200 RGBA); измерьте его на первой партии реальных рендеров и подставьте. Что **не допущение**: бюджет 500 МБ считается по `_dir_size(game/assets)` и краснит `vn build`.

### 8.2 Что растёт вместе с персонажами

| Что | Как | Порог/действие |
|---|---|---|
| `images.gen.rpy` | один `layeredimage` на персонажа, внутри — по строке `attribute` на каждый слой (`tools/vn/src/vn/content/images.py:101-237`); сейчас 1 690 Б на одного персонажа с 6 слоями | 50 персонажей × 39 слоёв ≈ 60–80 КБ `.rpy` + `.rpyc` — заметно, но не смертельно; всё это грузится и исполняется на init |
| `config.tag_layer` | по записи на персонажа (`images.py:243`) | линейно, безопасно |
| Валидация матрицы | дизъюнктность имён групп (`images.py:123-130`), `required`-комбинации обязаны существовать (`:132-147`), `forbidden` — не существовать (`:149-165`) | при `required: 3×4×8` линтер проверит 96 комбинаций на персонажа — правильное поведение, но растёт время линта |
| Декодированная память | 3 слоя × 10,07 МиБ на персонажа в кадре (§3.1) | **при 4 персонажах в кадре — 129 МиБ из дефолтных 400 МБ кэша** |
| Предзагрузка | не эмитится вообще | при большом касте первый показ каждой новой комбинации = фриз на декодировании (`ARCHITECTURE.md:2391`) |

### 8.3 Когда что понадобится

| Механизм | Статус | Когда становится обязательным |
|---|---|---|
| **Side images** (`spr/<char>/side/<emotion>@2.webp`) | **NOT IMPLEMENTED** — нормировано в `../conventions/naming.md:18` и `ARCHITECTURE.md:144,454,922`, но `grep "side/"` по `tools/vn/src/vn/` даёт **0 совпадений** | как только в диалогах появится портрет говорящего; ветку надо реализовать в `_discover` + эмиттере |
| **Оверлеи** (слёзы, румянец, эффекты) | **PARTIALLY IMPLEMENTED** — `sprite_tree` их сканирует (`pipeline.py:506`), эмиттер только предупреждает «эмиссия overlay-группы появится позже» (`images.py:178-182`) | сейчас нарисованные оверлеи собираются в `game/assets` и **едут мёртвым грузом в дистрибутив** — не рисуйте их, пока эмиссии нет |
| **Trim по bbox** | NOT IMPLEMENTED (`ARCHITECTURE.md:2409`) | при 3+ персонажах в кадре одновременно; даёт ~2× по памяти |
| **Частичная загрузка / выгрузка по главам** | NOT IMPLEMENTED | при касте, чей worst-case превышает 60 % `image_cache_size_mb` |
| **Атласы** | NOT IMPLEMENTED (`ARCHITECTURE.md:1157`) | только для покадровых VFX; для персонажей вреден — весь лист сидит в кэше целиком |
| **`vn char new` / `char validate`** | NOT IMPLEMENTED — заглушки фазы 1 (`cli.py:958`) | уже сейчас: каждый новый `character.yaml` пишется руками, ошибку поймает только линт |

## 9. Ассеты: сотни → тысячи

| Механизм | Поведение при росте | Код |
|---|---|---|
| Кэш `.vncache/assets` | фан-аут по 2 hex-символам = 256 каталогов, имя файла — полный 64-hex blake3. При 50 тыс. блобов ≈ 195 файлов на каталог — для NTFS нормально | `pipeline.py:312` |
| GC кэша | mark & sweep: строит live-множество из манифеста, затем `rglob("*")` по всем блобам — **линейный скан всего кэша** | `pipeline.py:458-491` |
| Рост кэша | неограничен: каждая правка сырца оставляет прошлый блоб навсегда | `vn assets cache --gc` вручную; в CI не вызывается нигде |
| Удаление осиротевших | **только** по диффу манифеста, дерево не сканируется. Файл, которого не было в манифесте, не удалится никогда; удалённый `.vncache/assets-manifest.json` навсегда отключает удаление (исключения проглатываются). Сам манифест с 2026-08-08 валидируется схемой `assets_manifest@1` при записи — битый документ теперь даёт красную сборку, а не тихую деградацию | `pipeline.py:419-433`, `:394-399`, `:441-455` |
| Холодная сборка | O(N) однопоточно, полное чтение всех сырцов (§4.1) | `pipeline.py:288-357` |
| Порог ADR-0004 | суммарный вес бинарей в `assets_src/` (из подсчёта исключены `.json/.yaml/.yml/.md/.txt/.gitkeep`): warn > 30 МБ, error > 50 МБ | `tools/vn/src/vn/content/lint.py:47`, `:371-400` |

**Самый близкий порог — именно ADR-0004.** Сейчас в `assets_src/` 11 бинарей на 129,3 КБ (плейсхолдеры). Реальный PNG-рендер 1200×2200 RGBA весит порядка 3–8 МБ. То есть:

```
warn (30 МБ)  ≈ после 4–10 реальных файлов
error (50 МБ) ≈ после 6–16 реальных файлов
```

Это буквально **один-два персонажа**. Значит переезд сырцов в хранилище — не «когда-нибудь», а часть подготовки к первой реальной партии рендеров. Механика хранилища (`vn assets lock` / `push` / `pull` / `status`, манифесты `asset_src@1`) — IMPLEMENTED, но **ни разу не запускалась**: `~/vn-assets-store` на машине не существует, `*.manifest.json` в репозитории ноль. S3-бэкенд — честная заглушка с внятным сообщением (`tools/vn/src/vn/assets/storage.py:129-133`). Детали — [Хранилище и бэкап](31-storage-and-backup.md).

**Локи при команде из нескольких человек** (`tools/vn/src/vn/assets/storage.py:99-134,178-209`):

| Свойство | Как есть | Риск |
|---|---|---|
| Где включается | **только** внутри `vn assets push` | `pull` без `--edit` лок не берёт и не проверяет; локальную правку ничто не останавливает |
| Атомарность | read-then-write, без `If-None-Match` (`ARCHITECTURE.md:293` требует условной записи) | два клиента в гонке оба «выигрывают» |
| Владелец | `git config user.name` с фолбэком на `getpass.getuser()` | подделывается одной командой; гарантия G14 процедурная, не техническая |
| TTL / эскалация / уведомления | NOT IMPLEMENTED (`ARCHITECTURE.md:295,903-906`) | забытый лок блокирует файл навсегда |
| `--force` | снимает чужой лок **без следа в аудите** | |

## 10. Локализация: 3 языка → 10

Механика раунд-трипа — [Локализация](14-localization.md). Здесь — арифметика роста.

Точный текущий срез (посчитано по файлам): `loc/po/en/common.po` — **96** записей, `ch01.po` — **16** (12 реплик + 4 пункта меню), `ch90.po` — **3**; итого **115**, что совпадает с `vn loc report` (de/en/pseudo — 115/115, fuzzy 0). UI-строк в `content/ui/strings.yaml` — **95**.

```
записей на язык ≈ 95 UI-строк + Σ по главам (реплики + пункты меню)
```

Удельные веса взяты из замеров: диалоговая запись PO ≈ 190 Б (`ch01.po` 3 036 Б / 16), UI-запись ≈ 130 Б (`common.po` 12 545 Б / 96), строка перевода в `game/tl` ≈ 90 Б плюс `.rpyc` ×1,5.

| Объём | Записей на язык | PO на язык | `game/tl/<lang>` (`.rpy`+`.rpyc`) |
|---|---|---|---|
| Сейчас | 115 | **15,8 КБ (замер)** | **≈13 КБ (замер)** |
| 1 реальная глава (300 реплик + 20 пунктов) | ~415 | ~79 КБ | ~70 КБ |
| 10 глав | ~3 300 | ~630 КБ | ~690 КБ |
| 50 глав | ~16 100 | ~3,1 МБ | ~3,4 МБ |
| **50 глав × 10 языков** | 161 000 единиц перевода | **~31 МБ PO в git** (текст, диффится нормально) | **~34 МБ в `game/tl`** |

Три следствия:

1. **`game/tl` не покрыт ни одним бюджетом**, а именно он растёт быстрее всего и **весь грузится движком на старте** (все языки, а не выбранный). Это и есть будущая деградация `Loading script`. Шардирование по главам уже правильное: `game/tl/<lang>/dialogue_<ch>.rpy` (`tools/vn/src/vn/loc/po.py:378-398`), так что перекомпилируются только изменившиеся файлы.
2. **Стоимость перевода линейна по новым и fuzzy-записям**, и это ровно то, что печатает `vn loc report`. Умножитель — число языков: `release_coverage_min: 0.98` (`loc/loc.yaml:7`) проверяется гейтом по **каждому** несинтетическому языку (`release.py:411-434`). Десятый язык — это не «ещё немного файлов», это десятый блокер релиза каждой главы.
3. **Риск переиспользования say-id — реальный и растёт с объёмом правок.** `used_nums` собирается из id, присутствующих **в текущем файле** (`tools/vn/src/vn/loc/keys.py:87-106`); удалили реплику — её номер освободился и достанется следующей новой реплике, которая унаследует чужой перевод из PO. High-watermark и ретайрмент из `ARCHITECTURE.md` — NOT IMPLEMENTED. Дешёвое лечение сейчас: не переиспользовать номера вручную и держать `loc/ledger/chNN.json` в ревью; правильное — реализовать high-watermark до того, как переводов станет много.

Ещё не реализовано и понадобится с ростом языков: RTL, цепочки фолбэка шрифтов, плюральные формы, локализуемые изображения, POT/msgmerge, push/pull в CAT-систему (все — NOT IMPLEMENTED).

## 11. Команда: 1 → 5 человек

### 11.1 Почему архитектура уже шардирована

Конфликты в git возникают там, где два человека правят один файл. Дерево спроектировано так, чтобы этого не случалось:

| Единица работы | Свой файл/каталог | Кто с кем не пересечётся |
|---|---|---|
| Глава | `content/chapters/ch<NN>_<slug>/` целиком | два сценариста на разных главах |
| Сцена | пара `sNNN_<slug>.scene.{yaml,rpy}` | два сценариста внутри одной главы |
| Переменные главы | `content/chapters/<ch>/vars.yaml` (store = `ch<NN>`) | без конфликтов в общем файле переменных |
| Реплики для перевода | `loc/ledger/ch<NN>.json` | переводчики по главам |
| Перевод | `loc/po/<lang>/ch<NN>.po` | язык × глава |
| Выход перевода | `game/tl/<lang>/dialogue_<ch>.rpy` | не в git вообще |
| Персонаж | `content/characters/<id>/character.yaml` | арт-директор vs сценарист |
| Пак/DLC | `packs/<id>/` — зеркало `content/` | команда DLC vs основная |

**И главное: весь генерат не в git** (`.gitignore` исключает `game/generated/`, `game/assets/`, `game/tl/`). Если бы 19 `.gen.rpy` + 19 `.rpyc` коммитились, каждый merge двух веток давал бы конфликт в бинарном `.rpyc` и в `manifest.json` — то есть конфликт при **любой** параллельной работе. Это, а не экономия места, — главная причина решения.

### 11.2 Что остаётся узким местом

| Файл | Почему конфликтует | Смягчение |
|---|---|---|
| `content/ui/strings.yaml` | один файл на все 95 UI-строк | правки редки; при росте — шардировать по доменам |
| `chapter.yaml: scene_order` | правится при каждой новой сцене главы | одна глава = один владелец (заготовка в `CODEOWNERS:25-26`) |
| `content/renames.yaml`, `content/registry/id_registry.json` | кросс-главные, правятся релизом | владельцы `@tech-lead` (`CODEOWNERS:15-16`) |
| `content/gallery/*.yaml`, `content/achievements/*.yaml` | один файл на всю игру | **в `CODEOWNERS` их нет вообще** |
| `project.yaml` | версия, бюджеты, флейворы | `@tech-lead @engine-dev-1` |
| `assets_src/` бинари | git не умеет мержить PNG | лок хранилища (§9) — но он не атомарен |

**Состояние `CODEOWNERS` — PARTIALLY IMPLEMENTED:** 19 правил (`CODEOWNERS:5-23`), но **все хэндлы — плейсхолдеры** (`@tech-lead`, `@engine-dev-1`, …; TODO прямо в шапке файла, `CODEOWNERS:3`). Не покрыты: `/content/gallery/`, `/content/achievements/`, `/content/ui/`, `/packs/`, `/assets_src/` и — важнее всего — **`/.github/`**, где живёт настоящий CI: 4 workflow, 7 определений джоб (на теге релизная `build` разворачивается матрицей флейворов в 2 прогона, итого до 8).

### 11.3 Порядок действий при найме второго человека

1. Заменить плейсхолдеры в `CODEOWNERS` на реальные хэндлы; добавить строки на непокрытые зоны и на конкретные главы по образцу `CODEOWNERS:26`.
2. Поднять хранилище сырцов на общем диске/NAS: `.vnstorage.yaml` сейчас указывает на `~/vn-assets-store` — **локальный путь**, для команды бессмысленный. Прогнать `vn assets lock` + `push` + `pull` хотя бы раз — механизм ни разу не исполнялся вживую.
3. Ввести правило «глава = ветка = один владелец» и держать `vn build --check` зелёным в PR (это уже делает `ci.yml`).
4. Проверить, что у каждого есть работающий `RENPY_SDK` — без него `vn build` падает чистой ошибкой компилятора (`tools/vn/src/vn/content/analyze.py:26-29`), а `vn bootstrap` собирает всё локально: удалённого кэша артефактов (G4) **нет**.

---

# Часть 3. Что решать сейчас, а что преждевременно

## 12. Принять сейчас (откатить потом дорого)

| Решение | Почему сейчас | Где живёт | Приоритет (предложение для [37-roadmap.md](37-roadmap.md)) |
|---|---|---|---|
| **Политика id (G7)** — нумерация глав/сцен, шаг `s010`, правила переименования | `id_registry.json` пуст только потому, что `ch01` — `draft` (`release.py:69-96`). С первой `status: release` главой id замораживаются навсегда, а исчезновение id становится ошибкой линта | `content/registry/id_registry.json`, `content/renames.yaml`, `tools/vn/src/vn/content/lint.py:319-369` | P0 |
| **Схемы (36 шт., конвенция `@N`)** | схема — контракт между декларацией и кодом; добавлять поля дёшево, менять смысл существующих — нет. Прецеденты обоих ходов уже есть: `assets_manifest@1` заведена и валидирует манифест при записи (`pipeline.py:441-455`) — нарушение G16 закрыто; `build_info@1` → `@2` бампнута из-за смысла поля (`patron_token` → `patron_tag`, ADR-0011), старая версия оставлена в реестре для чтения архивных артефактов | `tools/schemas/`, `tools/vn/src/vn/schemas.py:13-51` | P1 |
| **Именование ассетов** | имя зашито в путь, в ключ кэша, в имя Ren'Py-образа и — критично — в разблокировку галереи через `renpy.seen_image(image_name)`. Переименование CG **молча теряет разблокировку** (принято как цена в ADR-0010, кода-компенсации нет) | `../conventions/naming.md`, `pipeline.py:48`, `gallery.gen.rpy:12` | P0 |
| **Шардирование новых подсистем** | всё, что появится (реплики, квесты, музыкальная комната), должно шардироваться по главам, как ledger и tl. Один общий файл = гарантированные конфликты при 5 людях | §11.1 | P1 |
| **Провенанс с первого рендера** | цепочка «DAZ → ComfyUI → артефакт» восстанавливается только в момент генерации: seed, воркфлоу, LoRA. Код IMPLEMENTED и полностью протестирован, но в репозитории **ноль** `*.provenance.json` | `tools/vn/src/vn/assets/provenance.py`, гейт `release.py:365-373` | P0 |
| **Лицензионный реестр до закупок** | `content/licenses.yaml` уже гейтится релизом (`release.py:436-445`); ретроспективно доказывать `game_use` по 200 ассетам невозможно | `tools/vn/src/vn/assets/licenses.py:53-109` | P0 |
| **Переезд сырцов из git** | git append-only: 50 МБ мусора из истории потом убираются только переписыванием истории всей команде. Порог ADR-0004 достигается на первом-втором персонаже (§9) | `.vnstorage.yaml`, `tools/vn/src/vn/assets/storage.py` | P0 |
| **High-watermark say-id** | пока переводов 115 — правка дешёвая; после 100 тыс. единиц перевода коллизия id = тихо подставленный чужой текст | `tools/vn/src/vn/loc/keys.py:87-106` | P1 |
| **Гранулярность ключа AST-кэша** | сейчас ключ по всему набору файлов (`analyze.py:42-53`) — правка одной сцены перепарсивает все. Чинится в 10 строк сейчас и превращается в главный тормоз на 200 сценах | `tools/vn/src/vn/content/analyze.py` | P1 |
| **Бюджет на суммарный `.rpyc` + замер `Loading script`** | это то, что реально деградирует старт; `generated_total_kb` — лишь косвенная прокси-метрика | `release.py:29-54`, `ARCHITECTURE.md:89` | P2 |
| **`rpyc-cache` ключуется версией, а не флейвором** | `cli.py:358` — два флейвора одной версии затирают кэш друг друга локально; в GitHub это замаскировано ключами `actions/cache` | `cli.py:357-367` | P2 |

## 13. Преждевременно (не делать сейчас — и почему)

| Соблазн | Почему рано | Что делать вместо | Порог, после которого возвращаемся |
|---|---|---|---|
| **Атласы спрайтов/VFX** | покадровых последовательностей в проекте нет вообще (`seq/` не существует), а для персонажей атлас вреден: весь лист сидит в image cache целиком (`ARCHITECTURE.md:1157`) | миниатюры галереи (уже есть) и trim по bbox | появление покадровых VFX длиннее секунды |
| **S3-бэкенд хранилища** | `type: file` **ни разу не запускался** здесь: `~/vn-assets-store` не существует, манифестов ноль. Реализовывать облако поверх непроверенного контракта — отлаживать две системы сразу | прогнать `lock/push/pull/status` на общем диске или NAS; контракт манифестов при переходе не меняется (G21) | команда из 3+ человек в разных локациях или > 100 ГБ сырцов |
| **Инкрементальная компиляция по главам** | полный `vn build` — 0,3 с на прогретом кэше. Компилятор не боттлнек; боттлнек — Ren'Py-мост, и там чинится ключ кэша, а не компилятор | §12, «Гранулярность ключа AST-кэша» | `vn build` дольше 10 с на прогретом кэше |
| **Параллелизм `--jobs N` в сборке ассетов** | сборка сейчас миллисекунды; сложность (пул процессов + гонки за блобы + атомарность манифеста) не окупается | сперва убрать лишние чтения диска (§4.1), затем мерить | холодная сборка выходит за бюджет цикла художника P95 < 15 с (`ARCHITECTURE.md:1204`) |
| **Свой формат ассетов / AVIF / кастомный контейнер** | таблица трансформаций версионирована (`TRANSFORMS`, `pipeline.py:38-46`) — сменить формат позже стоит один бамп версии и одну холодную пересборку. Сейчас это чистый риск без выгоды | WebP/VP9 — то, что движок декодирует штатно | подтверждённый замер, что декодирование WebP тормозит на целевом железе |
| **`.rpa`-архивы** | сжатия не дают (§6.1): данные в RPA лежат сырыми, а WebP/WebM уже сжаты. `game/assets` = 0,067 МБ из 75,71 МБ дистрибутива | ничего | тысячи файлов в `game/assets` или поставка DLC отдельным файлом |
| **«Микросервисы» чего-либо, распределённая рендер-ферма, очередь задач** | одна машина, один человек, один GPU. Вся координация сейчас — файлы и git | `vn pipeline doctor` держит окружение в форме | вторая машина рендера в постоянной работе |
| **Свой профайлер / телеметрия рантайма** | движок уже пишет тайминги фаз в `log.txt`, а промахи image cache включаются одной переменной окружения: `config.debug_image_cache = "RENPY_DEBUG_IMAGE_CACHE" in os.environ` (`$RENPY_SDK/renpy/config.py:110`) | читать `log.txt`; `RENPY_DEBUG_IMAGE_CACHE=1 vn play`; поднять `nightly-perf` из `ARCHITECTURE.md:3640` | появление реальных отзывов игроков о производительности |

## Как изменить / как расширить

| Задача | Что править | Обязательно после |
|---|---|---|
| Поднять/опустить бюджет | `../../project.yaml:6-11` | прогнать `vn build` и `vn release validate --flavor public` — обе точки читают одно и то же (`release.py:29-54`) |
| Добавить новый бюджет (например `tl_total_kb`) | `budget_failures()` в `release.py:29-54` + ключ в `project.yaml` + `tools/schemas/project@1.schema.json` | тест по образцу `tools/vn/tests/test_release.py` (проверяет видео-бюджеты) |
| Внести `cold_start_s` в релизный гейт | `validate_release()` в `release.py:276-481` — сейчас проверок cold start там нет | учтите: гейт запускается без автопилота, понадобится вызвать прогон |
| Ускорить перекомпиляцию сцен | `tools/vn/src/vn/content/analyze.py:42-56` — считать ключ и кэш **на файл**, а не на набор | `python -m pytest tools/vn/tests/test_compile.py tools/vn/tests/test_scene_pipeline.py -q` |
| Шардировать smoke по главам | обвязка над `_autopilot_run` (`cli.py:1285-1344`) с `VN_AUTOPILOT_LOAD` (`030_flow.rpy:162-164`) и фикстурами `vn save corpus --add` | помните: фикстуры сейва валидны только против своей линии `.rpyc` (`ci/fixtures/rpyc-line/`) |
| Уменьшить генерат | убрать копию авторского источника в `tools/vn/src/vn/content/scenes.py:270-271` (~40 % объёма) | генерат перестанет быть самодостаточным при отладке; обновить [Сцены](12-scenes.md) |
| Ввести predict-списки | эмиссия данными (не построчными вызовами) в `tools/vn/src/vn/content/images.py`; норма — `ARCHITECTURE.md:2391` | бюджет на `.rpyc` обязателен вместе с этим |
| Включить `.rpa` | `build.archive(...)` в `../../game/options.rpy` | пересборка всего архива на каждую правку ассета; проверить патч-стратегию |

## Чего НЕ делать

- **Не считать, что «cold start 1,13 с» что-то доказывает.** Метрика снята на RTX с аппаратным GL, а бюджет калиброван по llvmpipe (`project.yaml:9`). Регрессию старта увидит nightly, не вы.
- **Не оптимизировать размер `game/assets`, пока он единицы мегабайт.** 97 % дистрибутива — движок и python (§6).
- **Не переключаться на `--profile draft` перед пушем.** Профиль входит в сравнение свежести — `vn build --check` покраснеет на всех выходах (`pipeline.py:309-311`).
- **Не полагаться на релизный гейт в вопросе качества энкода.** В `release.py` **нет ни одной ссылки на `profile`** — черновые q50/CRF42 ассеты пройдут все 19 проверок.
- **Не удалять `.vncache/assets-manifest.json` ради «чистой пересборки».** Он не в git и ниоткуда не восстанавливается; без него удаление осиротевших **навсегда** отключается, а сборка остаётся зелёной (`pipeline.py:394-399`).
- **Не оставлять `vn assets cache --gc` на потом.** Кэш растёт неограниченно и не подметается ни одной джобой CI.
- **Не рисовать оверлеи персонажей «на будущее»** — эмиссии `overlays`-группы нет (`images.py:178-182`), файлы соберутся и уедут в дистрибутив мёртвым грузом.
- **Не добавлять реальную главу, не подняв `--timeout` у smoke.** 300 реплик × 0,6 с ≈ 180 с — ровно дефолт (`cli.py:1350`), прогон снимут по таймауту и назовут это падением.
- **Не заливать в `assets_src/` реальные рендеры «на пробу».** Порог ADR-0004 в 50 МБ — это единицы файлов, а git append-only: убрать их потом = переписывание истории.
- **Не ставить `game/generated/`, `game/assets/`, `game/tl/` под git «чтобы у всех было одинаково».** Это гарантированный конфликт в `.rpyc` и `manifest.json` при любой параллельной работе (§11.1).
- **Не искать `vn test perf`, `vn validate --budgets`, `vn build --jobs/--use-artifact`** — ни одной из этих команд не существует, они есть только в `ARCHITECTURE.md`.

## Проверка

```bash
vn build                                  # бюджеты G19 на каждой сборке; «build: OK»
vn build --check                          # CI-режим: свежесть + бюджеты, ничего не пишет
vn release validate --flavor public       # 19 проверок, среди них #9 «бюджеты G19: в рамках»
vn test smoke --picks 0,0                 # печатает «cold start ... c» и валит прогон при > 30 c
vn assets cache --dry-run                 # объём кэша и сколько подметёт GC
vn assets video validate                  # per-file бюджет 40 МБ + валидность лупов
python -m pytest tools/vn/tests -q        # 152 теста, ~7,3 c (RENPY_SDK задан)
# фактические размеры (см. «Быстрый ответ»):
du -sh game/generated game/assets game/tl .vncache build/rpyc-cache
```

Эталон на 2026-08-08: `game/generated` 73,3 КБ / 39 файлов; `game/assets` 141,3 КБ / 20 файлов; `game/tl` 43,6 КБ / 3 языка; cold start 1,13 с; `vn build` 0,3 с; **152 теста за 7,25 с**; дистрибутив 0.1.0-public — 30,03 МиБ (75,71 МБ распакованный, 1640 файлов). Свежая patron-сборка после ADR-0011 — 1663 файла (сквозной прогон, ADR-0011 «Последствия»).

## Для AI-агента

| | |
|---|---|
| **Читать перед изменением** | `../../project.yaml` (бюджеты), `../../tools/vn/src/vn/release.py:29-54` (единственная реализация размерных бюджетов), `../../tools/vn/src/vn/cli.py:142,152,172-180` (вызовы из `vn build`), `../../tools/vn/src/vn/cli.py:1268-1344,1347-1401` (автопилот и cold start), `../../game/framework/00_core/030_flow.rpy:91-211` (`vn_qa`, `_T0`, тики), `../../tools/vn/src/vn/assets/pipeline.py:288-357,445-478` (модель стоимости сборки и GC), `../../tools/vn/src/vn/content/analyze.py:42-56` (ключ AST-кэша), `../../tools/vn/src/vn/content/compile.py:882-922` (свежесть и запись), `../../log.txt` (реальные тайминги старта) |
| **Не трогать** | `game/generated/**`, `game/assets/**`, `game/tl/**` — производные зоны, перезапишет сборка; `.vncache/**` — кэш и манифесты сборки (ручная правка `assets-manifest.json` ломает удаление осиротевших); `build/rpyc-cache/**` — носитель линии statement-имён (G6), правка ломает совместимость сейвов |
| **Зависимости (что ломается ниже по течению)** | Изменение бюджета в `project.yaml` меняет вердикт **и** `vn build`, **и** релизного гейта (проверка #9). Бамп версии в `TRANSFORMS` (`pipeline.py:38-46`) обесценивает весь кэш этой ветки и запускает холодную пересборку. Правка `tools/vn/src/vn/content/scenes.py:270-271` меняет размер каждого `*.gen.rpy` и, следовательно, `generated_total_kb`. Правка ключа в `analyze.py` инвалидирует все `.vncache/analyze-*.json` |
| **Валидация** | `vn build` → `vn build --check` → `vn test smoke --picks 0,0` → `python -m pytest tools/vn/tests -q` → `vn release validate --flavor public` |
| **Частые ошибки** | 1) Считать `docs/ARCHITECTURE.md` описанием построенного: `vn test perf`, `nightly-perf`, `vn validate --budgets`, `--jobs`, `--use-artifact`, атласы, trim, predict-эмиссия, бюджет памяти сцены, `.rpa` — **всё NOT IMPLEMENTED**. 2) Думать, что `cold_start_s` проверяется в CI на каждый PR — он проверяется только `vn test smoke`, которого нет ни в `ci.yml`, ни в релизном гейте. 3) Считать, что `@2` уменьшает потребление памяти — это только имя, декодируется полный размер. 4) Считать компилятор инкрементальным по главам — он пересчитывает всё, а AST-кэш ключуется по **всему набору** сцен. 5) Оценивать масштаб по числу комбинаций матрицы персонажа — файлы аддитивны (`poses × (1+outfits+emotions)`), а мультипликативна только память в кадре |
